更新blog
```bash
npx quartz sync
```

```ps1
function blogupdate {
    $targetPath = "C:\Users\tianyiwang\Documents\00-code\blog\bulesky29.github.io"
    if (Test-Path $targetPath -PathType Container) {
        Set-Location $targetPath
        Write-Host "'npx quartz sync'..." -ForegroundColor Green
        npx quartz sync
        Pop-Location
    } else {
        Write-Host "'$targetPath' Not exists" -ForegroundColor Red
    }
}


if (-not (Test-Path $PROFILE)) { New-Item -Path $PROFILE -ItemType File -Force }
notepad $PROFILE
```
